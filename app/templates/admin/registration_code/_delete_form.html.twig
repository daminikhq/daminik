<form method="post" action="{{ path('admin_registrationcode_delete', {'id': registration_code.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ registration_code.id) }}">
    <button class="btn">Delete</button>
</form>
